import React from 'react';
import HeaderCatalog from './Header/Header';

function Catalog() {
    return (
        <div className="catalog">
            <HeaderCatalog />
        </div>
    );
}

export default Catalog;