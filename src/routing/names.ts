export enum PUBLIC_PATH {
    APP = '/',
    NOTFOUND = '/404'
}

export enum USER_PATH {
    AUTH = '/auth',
    PRODUCT = '/auth/product/:id',
    CART = '/auth/cart',
    ORDERS = '/auth/orders'
}

export enum ADMIN_PATH {
    ADMIN = '/admin'
}

