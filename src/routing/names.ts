export enum PUBLIC_PATH {
    APP = '/',
    ADMIN = '/admin',
    NOTFOUND = '/404',
}

export enum AUTH_PATH {
    AUTH = '/auth',
    PRODUCT = '/auth/:id',
    CART = '/auth/cart',
    ORDERS = '/auth/orders'
}

